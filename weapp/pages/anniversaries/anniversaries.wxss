/* pages/anniversaries/anniversaries.wxss — 原型重设计 */
.page { height: 100vh; background: var(--bg); position: relative; }

/* ===== 页面Header ===== */
.page-header {
  background: linear-gradient(135deg, var(--primary) 0%, var(--primary-light) 100%);
  padding: 14px 16px;
  display: flex;
  align-items: center;
  position: relative;
  overflow: hidden;
}
.page-header::before {
  content: '';
  position: absolute;
  top: -30px; right: -20px;
  width: 120px; height: 120px;
  background: radial-gradient(circle, rgba(255,255,255,0.25) 0%, transparent 70%);
  border-radius: 50%;
}
.page-title {
  font-size: var(--text-h1-size);
  font-weight: var(--text-h1-weight);
  color: white;
  flex: 1;
  text-align: center;
  font-family: 'ZCOOL KuaiLe', cursive;
  letter-spacing: 2px;
  text-shadow: 1.5px 1.5px 0 rgba(212, 112, 138, 0.4);
}
.header-action {
  background: rgba(255,255,255,0.95);
  color: var(--primary-dark);
  padding: 6px 14px;
  border-radius: 14px 4px 14px 4px;
  font-size: 12px;
  font-weight: 600;
  box-shadow: 2px 2px 0 rgba(212, 112, 138, 0.25);
  border: 1.5px dashed var(--primary);
}

/* ===== 顶部统计英雄区 ===== */
.anniv-hero {
  padding: 20px 16px 14px;
  background: white;
  border-radius: 0 0 32px 16px;
  position: relative;
  box-shadow: 0 6px 0 rgba(232, 145, 164, 0.12);
}
.anniv-hero__row {
  display: flex;
  align-items: center;
  gap: 14px;
}
.anniv-hero__heart {
  width: 70px; height: 70px;
  background: linear-gradient(135deg, var(--primary) 0%, var(--primary-dark) 100%);
  border-radius: 50%;
  display: flex; align-items: center; justify-content: center;
  font-size: 36px;
  box-shadow: 4px 4px 0 rgba(212, 112, 138, 0.3);
  border: 3px dashed white;
  outline: 2px dashed var(--primary);
  outline-offset: -6px;
  flex-shrink: 0;
  animation: heartbeat 1.5s ease-in-out infinite;
}
@keyframes heartbeat {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.08); }
}
.anniv-hero__body { flex: 1; }
.anniv-hero__title {
  font-family: var(--font-deco);
  font-size: var(--text-h2-size);
  color: var(--text-primary);
  line-height: 1.1;
}
.anniv-hero__sub {
  font-size: var(--text-meta-size);
  color: var(--text-secondary);
  margin-top: 2px;
}
.anniv-hero__days {
  font-family: var(--font-deco);
  font-size: var(--text-h3-size);
  color: var(--primary-dark);
  font-weight: var(--text-h3-weight);
  text-align: right;
  line-height: 1.3;
  text-shadow: 2px 2px 0 rgba(232, 145, 164, 0.2);
}
.anniv-hero__days-unit {
  font-size: var(--text-h3-size);
  color: var(--text-secondary);
  font-weight: var(--text-h3-weight);
}

/* ===== 分类标签 ===== */
.anniv-tabs {
  display: flex;
  gap: 8px;
  padding: 14px 16px 8px;
  overflow-x: auto;
}
.anniv-tab {
  flex-shrink: 0;
  padding: 7px 16px;
  background: white;
  border: 1.5px dashed var(--card-border);
  border-radius: 18px 6px 18px 6px;
  font-size: 12px;
  color: var(--text-secondary);
  font-weight: 600;
  box-shadow: 2px 2px 0 rgba(232, 145, 164, 0.12);
  display: flex;
  align-items: center;
  gap: 4px;
}
.anniv-tab--active {
  background: linear-gradient(135deg, var(--primary) 0%, var(--primary-light) 100%);
  color: white;
  border-color: var(--primary-dark);
  transform: rotate(-1deg);
  box-shadow: 3px 3px 0 rgba(212, 112, 138, 0.3);
}
.anniv-tab__count {
  font-size: var(--text-tag-size);
  background: rgba(255,255,255,0.3);
  padding: 1px 6px;
  border-radius: 8px 2px 8px 2px;
}
.anniv-tab:not(.anniv-tab--active) .anniv-tab__count {
  background: var(--primary-pale);
  color: var(--primary-dark);
}

/* ===== 即将到来 ===== */
.anniv-soon {
  margin: 4px 16px 14px;
  padding: 14px 18px;
  background: linear-gradient(135deg, #FFE8E8 0%, #FFD9D9 100%);
  border: 2px dashed #FF6B6B;
  border-radius: 22px 10px 22px 10px;
  display: flex;
  align-items: center;
  gap: 12px;
  box-shadow: 4px 4px 0 rgba(255, 107, 107, 0.25);
  position: relative;
  overflow: hidden;
}
.anniv-soon::after {
  content: '🎉';
  position: absolute;
  top: -10px; right: -5px;
  font-size: 50px;
  opacity: 0.4;
  transform: rotate(15deg);
}
.anniv-soon__icon {
  width: 50px; height: 50px;
  background: white;
  border-radius: 14px 4px 14px 4px;
  display: flex; align-items: center; justify-content: center;
  font-size: 28px;
  border: 2px dashed #FF6B6B;
  box-shadow: 2px 2px 0 rgba(255, 107, 107, 0.3);
  flex-shrink: 0;
  z-index: 1;
}
.anniv-soon__body { flex: 1; z-index: 1; }
.anniv-soon__title {
  font-family: var(--font-deco);
  font-size: var(--text-h2-size);
  color: #FF6B6B;
  font-weight: var(--text-h2-weight);
  line-height: 1.1;
}
.anniv-soon__sub {
  font-size: var(--text-meta-size);
  color: var(--text-secondary);
  margin-top: 2px;
}
.anniv-soon__days {
  font-family: var(--font-deco);
  font-size: var(--text-h3-size);
  color: #FF6B6B;
  font-weight: var(--text-h3-weight);
  text-align: right;
  z-index: 1;
  text-shadow: 2px 2px 0 rgba(255, 107, 107, 0.2);
  flex-shrink: 0;
}

/* ===== 纪念日卡片 ===== */
.anniv-list { padding: 0 16px 100px; }
.anniv-item {
  margin-bottom: 14px;
  background: white;
  border: 2px dashed var(--card-border);
  border-radius: 24px 10px 24px 10px;
  padding: 14px 16px;
  display: flex;
  align-items: center;
  gap: 14px;
  box-shadow: 5px 5px 0 rgba(232, 145, 164, 0.15);
  position: relative;
  overflow: hidden;
}
.anniv-item::before {
  content: '';
  position: absolute;
  top: 0; right: 0;
  width: 80px; height: 80px;
  background: radial-gradient(circle, currentColor 0%, transparent 70%);
  opacity: 0.1;
  border-radius: 50%;
  pointer-events: none;
}
.anniv-item--pink { --c: var(--c-pink, #E891A4); }
.anniv-item--red { --c: var(--c-red, #FF6B6B); }
.anniv-item--purple { --c: var(--c-purple, #B19CD9); }
.anniv-item--orange { --c: var(--c-orange, #FF8E53); }
.anniv-item--blue { --c: var(--c-blue, #7BC4E8); }
.anniv-item--green { --c: var(--c-green, #7BC67B); }
.anniv-item--yellow { --c: #C49B00; }

.anniv-item__art {
  width: 64px; height: 64px;
  flex-shrink: 0;
  background: var(--primary-pale);
  border-radius: 18px 6px 18px 6px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 36px;
  border: 2px dashed var(--c);
  position: relative;
  z-index: 1;
}
.anniv-item--red .anniv-item__art { background: #FFE8E8; }
.anniv-item--purple .anniv-item__art { background: #F0E8FF; }
.anniv-item--orange .anniv-item__art { background: #FFE8D9; }
.anniv-item--blue .anniv-item__art { background: #E0F4FF; }
.anniv-item--green .anniv-item__art { background: #E8F5E8; }
.anniv-item--yellow .anniv-item__art { background: #FFF8D9; }

.anniv-item__body { flex: 1; min-width: 0; position: relative; z-index: 1; }
.anniv-item__row {
  display: flex;
  align-items: center;
  gap: 6px;
  margin-bottom: 2px;
}
.anniv-item__name {
  font-size: var(--text-h3-size);
  color: var(--text-primary);
  font-weight: var(--text-h3-weight);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
.anniv-item__tag {
  font-size: var(--text-tag-size);
  color: white;
  padding: 1px 6px;
  border-radius: 6px 2px 6px 2px;
  font-weight: 700;
  flex-shrink: 0;
  letter-spacing: 0.5px;
}
.anniv-item__date {
  font-size: var(--text-body-size);
  color: var(--text-secondary);
  font-family: var(--font-body);
  letter-spacing: 0.5px;
}
.anniv-item__countdown {
  text-align: right;
  flex-shrink: 0;
  position: relative;
  z-index: 1;
}
.anniv-item__days {
  font-family: var(--font-deco);
  font-size: var(--text-h3-size);
  color: var(--c);
  font-weight: var(--text-h3-weight);
  line-height: 1;
}
.anniv-item__days--soon { color: #FF6B6B; }
.anniv-item__days--past { color: var(--text-light); }
.anniv-item__days-unit {
  font-size: var(--text-h3-size);
  color: var(--text-secondary);
  margin-top: 2px;
  font-weight: var(--text-h3-weight);
  text-align: center;
}
.anniv-item__del {
  position: absolute;
  top: 8px; right: 8px;
  width: 22px; height: 22px;
  background: white;
  color: var(--text-light);
  border-radius: 50%;
  display: flex; align-items: center; justify-content: center;
  font-size: 14px;
  border: 1.5px dashed #E0E0E0;
  line-height: 1;
  font-weight: 700;
  z-index: 2;
}

/* ===== FAB ===== */
.fab {
  position: fixed;
  bottom: 40px;
  left: 50%;
  transform: translateX(-50%);
  width: 56px; height: 56px;
  background: linear-gradient(135deg, var(--primary) 0%, var(--primary-dark) 100%);
  color: white;
  border-radius: 22px 8px 22px 8px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 32px;
  box-shadow: 4px 4px 0 rgba(212, 112, 138, 0.4);
  border: 2.5px dashed white;
  outline: 2px dashed var(--primary);
  outline-offset: -8px;
  font-weight: 300;
  z-index: 50;
}

/* ===== 以下为原有弹窗样式（保持不动） ===== */
.holiday-picker {
  background: white;
  border-radius: 16px 16px 0 0;
  max-height: 70vh;
  display: flex;
  flex-direction: column;
  box-shadow: 0 -4px 24px rgba(0,0,0,0.08);
}
.holiday-picker-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 16px 18px 12px;
  border-bottom: 1px solid #f0e8ec;
}
.holiday-picker-title {
  font-size: 16px;
  font-weight: 700;
  color: #333;
}
.holiday-picker-close {
  font-size: 18px;
  color: #999;
  padding: 4px 8px;
}
.holiday-picker-list {
  flex: 1;
  overflow-y: auto;
  padding: 8px 0;
}
.holiday-item {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 10px 18px;
}
.holiday-item--disabled { opacity: 0.45; }
.holiday-checkbox { transform: scale(0.85); }
.holiday-emoji { font-size: 22px; width: 30px; text-align: center; }
.holiday-info { flex: 1; }
.holiday-title { font-size: 14px; font-weight: 600; color: #333; display: block; }
.holiday-date { font-size: var(--text-body-size); color: #999; display: block; margin-top: 1px; }
.holiday-tag {
  font-size: 10px;
  color: #999;
  background: #f5f0f2;
  padding: 2px 8px;
  border-radius: 10px;
}
.holiday-picker-footer {
  padding: 12px 18px;
  border-top: 1px solid #f0e8ec;
}

/* ===== 日期选择器 ===== */
.overlay--picker-bottom {
  display: flex;
  align-items: flex-end;
  justify-content: center;
  background: rgba(60, 40, 50, 0.5);
}
.date-picker-sheet {
  width: 100%;
  max-height: 70vh;
  background: white;
  border-radius: 32rpx 32rpx 0 0;
  display: flex;
  flex-direction: column;
  box-shadow: 0 -8rpx 32rpx rgba(0,0,0,0.08);
  padding-bottom: env(safe-area-inset-bottom);
}
.date-picker-bar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 20rpx 28rpx 12rpx;
  border-bottom: 2rpx dotted #F0E8EC;
}
.date-picker-link {
  font-size: 26rpx;
  color: var(--text-secondary, #8B7A8E);
  padding: 8rpx 4rpx;
  font-weight: 500;
}
.date-picker-link--strong {
  color: var(--primary, #E891A4);
  font-weight: 700;
}
.date-picker-title {
  font-size: 28rpx;
  color: var(--text-primary, #5D4E60);
  font-weight: 700;
  font-family: 'ZCOOL KuaiLe', cursive;
}
.date-picker-view {
  width: 100%;
  height: 360rpx;
  text-align: center;
}
.date-picker-item {
  font-size: 32rpx;
  color: var(--text-primary, #5D4E60);
  font-weight: 600;
  font-family: 'ZCOOL KuaiLe', cursive;
  line-height: 44px;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
