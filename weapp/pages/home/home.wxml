<view class="app-root app-theme-{{uiTheme}} page">
  <scroll-view class="home-scroll" scroll-y show-scrollbar="{{false}}">

    <!-- 头部：情侣信息 + 徽章 + 三栏统计 -->
    <view class="home-header">
      <view class="page-nav" style="padding-top: {{statusBarHeight}}px">
        <view class="page-nav__bar" style="height: {{navBarHeight}}px">
          <!-- <text class="page-nav__title">一起攒</text> -->
        </view>
      </view>
      <view class="couple-info">
        <view class="avatar avatar--me">{{myInitial}}</view>
        <view class="couple-info-text">
          <text class="couple-name">{{coupleDisplayName || userInfo.nickname || '小可爱'}}</text>
          <text class="couple-days" wx:if="{{partner}}">已在一起 {{together_days}} 天</text>
          <text class="couple-days couple-days--bind" wx:else bindtap="goBind">绑定伴侣，开启双人攒钱</text>
        </view>
      </view>
      <view class="home-badges">
        <view class="badge {{sparkStatus === 'gray' ? '' : 'badge--active'}}">
          <text>火花 {{sparkCount}} 天</text>
        </view>
        <view class="badge" bindtap="go" data-url="/pages/level/level">
          <text>Lv.{{level}}</text>
        </view>
      </view>
      <view class="stats-row">
        <view class="stat-card">
          <text class="stat-card__value">{{delivered_text}}</text>
          <text class="stat-card__label">已存金额</text>
        </view>
        <view class="stat-card">
          <text class="stat-card__value">{{active_plan_count}}</text>
          <text class="stat-card__label">进行中</text>
        </view>
        <view class="stat-card">
          <text class="stat-card__value">{{level}}</text>
          <text class="stat-card__label">等级</text>
        </view>
      </view>
    </view>

    <!-- 快捷入口 -->
    <view class="quick-actions">
      <view class="quick-action" bindtap="goTab" data-url="/pages/plans/plans" hover-class="quick-action--press">
        <view class="quick-action__icon quick-action__icon--rose"><cp-icon name="wallet" size="44" /></view>
        <text class="quick-action__label">存钱</text>
      </view>
      <view class="quick-action" bindtap="go" data-url="/pages/gacha/gacha" hover-class="quick-action--press">
        <view class="quick-action__icon quick-action__icon--gold"><cp-icon name="gift" size="44" /></view>
        <text class="quick-action__label">抽卡</text>
      </view>
      <view class="quick-action" bindtap="go" data-url="/pages/anniversaries/anniversaries" hover-class="quick-action--press">
        <view class="quick-action__icon quick-action__icon--lavender"><cp-icon name="calendar" size="44" /></view>
        <text class="quick-action__label">纪念日</text>
      </view>
      <view class="quick-action" bindtap="go" data-url="/pages/notes/notes" hover-class="quick-action--press">
        <view class="quick-action__icon quick-action__icon--sky"><cp-icon name="envelope" size="44" /></view>
        <text class="quick-action__label">留言</text>
      </view>
    </view>

    <!-- 抽签小助手 -->
    <view class="section-header">
      <text class="section-title">🎯 抽签小助手</text>
      <text class="section-more" bindtap="go" data-url="/pages/decision/decision">去抽签 ›</text>
    </view>
    <view class="dc-home-card" bindtap="go" data-url="/pages/decision/decision" hover-class="dc-home-card--press">
      <view class="dc-home-card__bg"></view>
      <view class="dc-home-card__row">
        <text class="dc-home-card__emoji">🎲</text>
        <view class="dc-home-card__text">
          <text class="dc-home-card__title">选择困难？让天意帮你决定</text>
          <text class="dc-home-card__sub">吵架了 · 吃什么 · 去哪玩 · 纪念日 · 感情降温</text>
        </view>
      </view>
      <view class="dc-home-card__arrow">›</view>
    </view>

    <!-- 签到 -->
    <view class="checkin-mini {{checkinAnim}}" bindtap="onCheckinTap">
      <view class="checkin-mini-left">
        <view class="checkin-mini-icon">
          <cp-icon name="{{checkedInToday ? 'sparkles' : 'clock'}}" size="32" />
        </view>
        <view>
          <text class="checkin-mini-text">{{checkedInToday ? '今日已签到' : '完成今日签到'}}</text>
          <text class="checkin-mini-sub">连续 {{sparkCount}} 天 · 每日 +5 积分</text>
        </view>
      </view>
      <view class="checkin-mini-btn {{checkedInToday ? 'checkin-mini-btn--done' : ''}}">
        {{checkedInToday ? '已签到' : '签到'}}
      </view>
    </view>

    <!-- 存钱计划 -->
    <view class="section-header">
      <text class="section-title">存钱计划</text>
      <text class="section-more" bindtap="goTab" data-url="/pages/plans/plans">查看全部 ›</text>
    </view>

    <block wx:if="{{featuredPlan}}">
      <view class="plan-card">
        <view class="plan-header">
          <text class="plan-title">{{featuredPlan.title}}</text>
          <text class="plan-tag">{{featuredPlan.done ? '已完成' : '进行中'}}</text>
        </view>
        <view class="plan-progress">
          <view class="progress-bar">
            <view class="progress-fill" style="width:{{featuredPlan.progressPct}}%"></view>
          </view>
          <view class="progress-info">
            <text>{{featuredPlan._curText}} / {{featuredPlan._tarText}}</text>
            <text>{{featuredPlan.progressPctText}}%</text>
          </view>
        </view>
        <view class="plan-footer">
          <text class="plan-date">{{featuredPlan.deadline_date || '未设截止日期'}}</text>
          <view wx:if="{{!featuredPlan.done}}" class="plan-action-btn" catchtap="goTab" data-url="/pages/plans/plans">记一笔</view>
        </view>
      </view>
    </block>
    <view wx:else class="plan-card plan-card--empty" bindtap="goTab" data-url="/pages/plans/plans">
      <cp-icon name="target" size="48" />
      <text class="plan-empty-title">还没有存钱计划</text>
      <text class="plan-empty-hint">设定一个小目标，从今天开始</text>
    </view>

    <!-- 纪念日 -->
    <view wx:if="{{nearestAnni || todayAnni}}" class="anniversary-section">
      <view class="section-header section-header--compact">
        <text class="section-title">即将到来</text>
        <text class="section-more" bindtap="go" data-url="/pages/anniversaries/anniversaries">全部 ›</text>
      </view>
      <view class="anniversary-card" bindtap="go" data-url="/pages/anniversaries/anniversaries">
        <view class="anniversary-icon">
          <cp-icon name="heart" size="40" color="#fff" />
        </view>
        <view class="anniversary-info">
          <text class="anniversary-name">{{todayAnni ? todayAnni.title : nearestAnni.title}}</text>
          <text class="anniversary-date">{{todayAnni ? '值得庆祝的一天' : nearestAnni.date}}</text>
        </view>
        <view class="anniversary-days">
          <text class="anniversary-days__num">{{todayAnni ? 0 : nearestAnni.days}}</text>
          <text class="anniversary-days__unit">天</text>
        </view>
      </view>
    </view>

    <!-- 卡片任务 -->
    <view wx:if="{{cardTasksAsAssigner.length > 0}}" class="block block--tasks">
      <view class="section-header section-header--compact">
        <text class="section-title">我发起的任务</text>
      </view>
      <view class="task-card">
        <view wx:for="{{cardTasksAsAssigner}}" wx:key="id" class="ct-item ct-assigner {{item.card_item_id === 'forgive_me' ? 'ct-item--forgive' : ''}} {{item.card_item_id === 'please_forgive_me' ? 'ct-item--please-forgive' : ''}}">
          <view class="ct-body">
            <text class="ct-icon">{{item.card_item_id === 'forgive_me' ? '🥺' : item.card_item_id === 'please_forgive_me' ? '💎' : '📋'}}</text>
            <view class="ct-info">
              <text class="ct-name">{{item.card_name || '任务'}}</text>
              <text class="ct-desc">{{item.task_display_text}}</text>
            </view>
          </view>
          <view wx:if="{{item.card_item_id === 'forgive_me' && item.rejected && item.status === 'pending'}}" class="ct-actions">
            <button class="ct-btn ct-btn-retry" bindtap="onCardRetry" data-id="{{item.id}}">再次请求</button>
          </view>
          <view wx:elif="{{item.card_item_id === 'forgive_me' && item.status === 'completed'}}" class="ct-forgive-completed" bindtap="onCardForgive" data-id="{{item.id}}" data-mode="ack">对方原谅你了，点我确认</view>
          <view wx:elif="{{item.card_item_id === 'please_forgive_me' && item.rejected && item.status === 'pending'}}" class="ct-actions ct-actions--please">
            <button class="ct-btn ct-btn-retry ct-btn--please" bindtap="onCardRetry" data-id="{{item.id}}">再次诚挚请求</button>
          </view>
          <view wx:elif="{{item.card_item_id === 'please_forgive_me' && item.status === 'completed'}}" class="ct-forgive-completed ct-forgive-completed--please" bindtap="onCardForgive" data-id="{{item.id}}" data-mode="ack">对方原谅你了，快去找他当面认错吧</view>
          <view wx:elif="{{item.status === 'completed_pending'}}" class="ct-actions">
            <button class="ct-btn ct-btn-confirm" bindtap="onCardConfirm" data-id="{{item.id}}">确认完成</button>
            <button class="ct-btn ct-btn-dispute" bindtap="onCardDispute" data-id="{{item.id}}">未完成</button>
          </view>
          <view wx:elif="{{item.status === 'disputed'}}" class="ct-actions">
            <text class="ct-waiting-text">等待对方重新完成</text>
          </view>
        </view>
      </view>
    </view>

    <view wx:if="{{cardTasksAsAssignee.length > 0}}" class="block block--tasks">
      <view class="section-header section-header--compact">
        <text class="section-title">指派给我的任务</text>
      </view>
      <view class="task-card">
        <view wx:for="{{cardTasksAsAssignee}}" wx:key="id" class="ct-item ct-assignee {{item.card_item_id === 'forgive_me' ? 'ct-item--forgive' : ''}} {{item.card_item_id === 'please_forgive_me' ? 'ct-item--please-forgive' : ''}}">
          <view class="ct-body">
            <text class="ct-icon">{{item.card_item_id === 'forgive_me' ? '🥺' : item.card_item_id === 'please_forgive_me' ? '💎' : '📋'}}</text>
            <view class="ct-info">
              <text class="ct-name">{{item.card_name || '任务'}}</text>
              <text class="ct-desc">{{item.task_display_text}}</text>
            </view>
          </view>
          <view wx:if="{{item.card_item_id === 'forgive_me' && !item.rejected && item.status === 'pending'}}" class="ct-actions">
            <button class="ct-btn ct-btn-forgive" bindtap="onCardForgive" data-id="{{item.id}}">愿意原谅</button>
            <button class="ct-btn ct-btn-reject" bindtap="onCardReject" data-id="{{item.id}}">不愿意</button>
          </view>
          <view wx:elif="{{item.card_item_id === 'forgive_me' && item.rejected}}" class="ct-status-text">已收到，等待你的决定</view>
          <view wx:elif="{{item.card_item_id === 'forgive_me' && item.status === 'completed_pending'}}" class="ct-status-text">等待对方确认中…</view>
          <view wx:elif="{{item.card_item_id === 'forgive_me' && item.status === 'completed'}}" class="ct-done-badge">已原谅</view>
          <view wx:elif="{{item.card_item_id === 'please_forgive_me' && !item.rejected && item.status === 'pending'}}" class="ct-actions ct-actions--please">
            <button class="ct-btn ct-btn-forgive ct-btn--please" bindtap="onCardForgive" data-id="{{item.id}}">原谅TA</button>
            <button class="ct-btn ct-btn-reject" bindtap="onCardReject" data-id="{{item.id}}">不愿意</button>
          </view>
          <view wx:elif="{{item.card_item_id === 'please_forgive_me' && item.rejected}}" class="ct-status-text ct-status-text--please">等待TA的再次诚挚道歉…</view>
          <view wx:elif="{{item.card_item_id === 'please_forgive_me' && item.status === 'completed_pending'}}" class="ct-status-text">等待对方确认中…</view>
          <view wx:elif="{{item.card_item_id === 'please_forgive_me' && item.status === 'completed'}}" class="ct-done-badge ct-done-badge--please">已原谅</view>
          <view wx:elif="{{item.status === 'pending' && item.card_item_id !== 'serve_me'}}" class="ct-actions">
            <button class="ct-btn ct-btn-complete" bindtap="onCardComplete" data-id="{{item.id}}">已完成</button>
            <button class="ct-btn ct-btn-decline" bindtap="onCardDecline" data-id="{{item.id}}">我不要</button>
          </view>
          <view wx:elif="{{item.status === 'pending' && item.card_item_id === 'serve_me'}}" class="ct-actions">
            <button class="ct-btn ct-btn-complete" bindtap="onCardComplete" data-id="{{item.id}}">已完成</button>
          </view>
          <view wx:elif="{{item.status === 'disputed' && item.card_item_id !== 'serve_me'}}" class="ct-actions">
            <button class="ct-btn ct-btn-complete" bindtap="onCardComplete" data-id="{{item.id}}">已完成</button>
            <button class="ct-btn ct-btn-decline" bindtap="onCardDecline" data-id="{{item.id}}">我不要</button>
          </view>
          <view wx:elif="{{item.status === 'disputed' && item.card_item_id === 'serve_me'}}" class="ct-actions">
            <button class="ct-btn ct-btn-complete" bindtap="onCardComplete" data-id="{{item.id}}">已完成</button>
          </view>
        </view>
      </view>
    </view>

    <!-- 宠物 -->
    <view class="pet-area">
      <view class="section-header section-header--compact">
        <text class="section-title">我们的宠物</text>
        <text wx:if="{{pet}}" class="section-more" bindtap="goPetPage">宠物管理 ›</text>
        <text wx:else class="section-more" bindtap="go" data-url="/pages/gacha/gacha">去抽卡 ›</text>
      </view>

      <view wx:if="{{pet}}" class="pet-card">
        <view class="pet-bg"></view>
        <view class="pet-avatar {{petAnimationClass}} {{petActionClass}} {{petFeedClass}}" catchtap="onPetTap">
          <text wx:if="{{showActionEmoji}}" class="pet-action-emoji {{actionEmojiClass}}">{{actionEmoji}}</text>
          <image wx:if="{{petImageUrl}}" class="pet-avatar-img" src="{{petImageUrl}}" mode="aspectFit"></image>
          <text wx:else class="pet-avatar-emoji">{{petEmoji}}</text>
        </view>
        <text class="pet-name">{{petName}}</text>
        <text class="pet-level">{{petRarity}} · Lv.{{petLevel}}</text>
        <view class="pet-interact-badge" wx:if="{{petId}}">
          <text>今日互动</text>
          <view class="pet-interact-dot {{todayInteractTotal >= 1 ? '' : 'pet-interact-dot--used'}}"></view>
          <view class="pet-interact-dot {{todayInteractTotal >= 2 ? '' : 'pet-interact-dot--used'}}"></view>
          <view class="pet-interact-dot {{todayInteractTotal >= 3 ? '' : 'pet-interact-dot--used'}}"></view>
          <text>{{todayInteractTotal}}/3</text>
        </view>
        <view class="pet-intimacy">
          <view class="intimacy-bar">
            <view class="intimacy-fill" style="width:{{intimacyPct}}%"></view>
          </view>
          <text class="intimacy-text">{{intimacyPct}}/100</text>
        </view>
        <view class="pet-actions">
          <view class="pet-action" bindtap="onFeed">
            <view class="pet-action-icon"><text>喂</text></view>
            <text class="pet-action-label">喂食</text>
          </view>
          <view class="pet-action" bindtap="onStroke">
            <view class="pet-action-icon"><text>摸</text></view>
            <text class="pet-action-label">抚摸</text>
          </view>
          <view class="pet-action" bindtap="onWalk">
            <view class="pet-action-icon"><text>散</text></view>
            <text class="pet-action-label">散步</text>
          </view>
          <view class="pet-action" bindtap="onPlay">
            <view class="pet-action-icon"><text>玩</text></view>
            <text class="pet-action-label">玩耍</text>
          </view>
        </view>
      </view>

      <view wx:else class="pet-card pet-card--empty" bindtap="go" data-url="/pages/gacha/gacha">
        <view class="pet-bg"></view>
        <cp-icon name="sparkles" size="56" />
        <text class="pet-empty-title">还没有宠物伙伴</text>
        <text class="pet-empty-hint">抽卡获得专属萌宠，一起攒钱成长</text>
      </view>
    </view>

    <view class="home-bottom-spacer"></view>
  </scroll-view>

  <!-- 弹窗 -->
  <view wx:if="{{showPetModal}}" class="modal-overlay" bindtap="hidePetModal">
    <view class="pet-modal" catchtap="stopPropagation">
      <view class="pet-modal-handle"></view>
      <view class="pet-modal-visual">
        <image wx:if="{{petImageUrl}}" class="pet-modal-img" src="{{petImageUrl}}" mode="aspectFit"></image>
        <text wx:else class="pet-modal-emoji">{{petEmoji}}</text>
      </view>
      <text class="pet-modal-name">{{petName}}</text>
      <view class="pet-modal-stat">
        <text class="pet-modal-stat-label">亲密度</text>
        <view class="pet-modal-bar"><view class="pet-modal-fill" style="width:{{intimacyPct}}%"></view></view>
        <text class="pet-modal-stat-val">{{intimacyPct}}%</text>
      </view>
      <view class="pet-modal-stat" wx:if="{{!petEvolutionReady}}">
        <text class="pet-modal-stat-label">经验 Lv.{{petLevel}}</text>
        <view class="pet-modal-bar"><view class="pet-modal-fill" style="width:{{petExpNeeded > 0 ? (petExp / petExpNeeded * 100) : 0}}%"></view></view>
        <text class="pet-modal-stat-val">{{petExp}}/{{petExpNeeded}}</text>
      </view>
      <text wx:elif="{{petEvolutionReady}}" class="pet-modal-evo">可进化 · 前往宠物管理</text>
      <text class="pet-modal-desc">{{petDescription}}</text>
      <button class="pet-modal-btn" bindtap="feedCurrentPet">投喂宠物</button>
      <text class="pet-modal-close" bindtap="hidePetModal">关闭</text>
    </view>
  </view>

  <view wx:if="{{showLevelUp}}" class="levelup-overlay">
    <view class="levelup-modal">
      <view class="levelup-ring"></view>
      <text class="levelup-kicker">LEVEL UP</text>
      <text class="levelup-lv">Lv.{{levelUpNewLevel}}</text>
      <text class="levelup-sub">情侣等级提升</text>
    </view>
  </view>

  <view wx:if="{{showAdventure && adventureData}}" class="levelup-overlay" bindtap="hideAdventure">
    <view class="adventure-modal" catchtap="stopPropagation">
      <text class="adventure-emoji">{{adventureData.pet_emoji}}</text>
      <text class="adventure-title">{{adventureData.pet_name}} 冒险归来</text>
      <text class="adventure-desc">{{adventureData.passive_name}}</text>
      <view class="adventure-rewards">
        <view wx:if="{{adventureData.reward.shards > 0}}" class="adventure-reward">
          <text class="ar-num">+{{adventureData.reward.shards}}</text>
          <text class="ar-label">积分</text>
        </view>
        <view wx:if="{{adventureData.reward.exp > 0}}" class="adventure-reward">
          <text class="ar-num">+{{adventureData.reward.exp}}</text>
          <text class="ar-label">经验</text>
        </view>
        <view wx:if="{{adventureData.reward.tickets > 0}}" class="adventure-reward">
          <text class="ar-num">+{{adventureData.reward.tickets}}</text>
          <text class="ar-label">抽卡券</text>
        </view>
      </view>
      <view wx:if="{{adventureData.week_summary}}" class="adventure-week">
        <text>本周累计 · 积分 {{adventureData.week_summary.total_shards}} · 经验 {{adventureData.week_summary.total_exp}}</text>
      </view>
      <text class="adventure-tap">轻触关闭</text>
    </view>
  </view>

  <view wx:if="{{isGuest}}" class="guest-overlay" bindtap="onGuestAction">
    <view class="guest-card">
      <view class="guest-card-icon">🔒</view>
      <text class="guest-card-title">登录后使用全部功能</text>
      <text class="guest-card-desc">共同存钱、宠物养成、每日签到</text>
      <button class="guest-card-btn">去登录</button>
    </view>
  </view>
</view>
