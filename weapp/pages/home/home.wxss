/* 首页 — 一起攒 · 新布局 */
.page { height: 100vh; background: transparent; }

/* ===== 1. 伴侣+等级 ===== */
.compact-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 12px 16px 8px;
}
.ch-left { flex: 1; }
.ch-name { font-size: 20px; font-weight: 800; color: var(--text); display: inline; }
.ch-partner { font-size: 13px; color: var(--text-secondary); margin-left: 6px; }
.ch-bind { background: rgba(214, 92, 138, 0.12); padding: 2px 8px; border-radius: 10px; }
.ch-days { font-size: 11px; color: var(--text-muted); margin-left: 4px; }
.ch-spark { font-size: 11px; color: #ff6b35; margin-left: 4px; font-weight: 700; }
.ch-level { text-align: center; }
.ch-lv {
  background: rgba(214, 92, 138, 0.12);
  padding: 3px 12px;
  border-radius: 12px;
  font-size: 12px;
  font-weight: 700;
  color: var(--pink-dark);
  display: inline-block;
}
.ch-lv-bar { width: 48px; height: 3px; background: rgba(214, 92, 138, 0.2); border-radius: 2px; margin: 4px auto 0; overflow: hidden; }
.ch-lv-fill { height: 100%; background: var(--pink-dark); border-radius: 2px; }

/* ===== 2. 攒钱总进度（核心大卡片） ===== */
.money-card {
  margin: 4px 12px 8px;
  background: linear-gradient(135deg, #ffffff 0%, #fff8fc 55%, #ffe8f2 100%);
  border-radius: var(--radius-lg);
  padding: 20px;
  box-shadow: 0 8px 24px rgba(214, 92, 138, 0.12);
  border: 1px solid #ffd6e8;
  position: relative;
  overflow: hidden;
}
.money-card::before {
  content: '';
  position: absolute;
  top: -40px; right: -30px;
  width: 120px; height: 120px;
  border-radius: 50%;
  background: radial-gradient(circle, rgba(214, 92, 138, 0.08) 0%, transparent 70%);
}
.money-top { display: flex; justify-content: space-between; align-items: flex-start; }
.money-info { flex: 1; }
.money-label { font-size: 12px; color: var(--text-secondary); display: block; }
.money-amount { font-size: 32px; font-weight: 900; color: var(--pink-dark); display: block; margin-top: 2px; letter-spacing: -1px; }
.money-target { font-size: 12px; color: var(--text-muted); margin-top: 2px; display: block; }
.money-icon { font-size: 32px; opacity: .6; }
.money-bar {
  height: 8px;
  background: rgba(214, 92, 138, 0.15);
  border-radius: 4px;
  margin-top: 14px;
  overflow: hidden;
}
.money-bar-fill {
  height: 100%;
  background: linear-gradient(90deg, var(--pink), var(--pink-dark));
  border-radius: 4px;
  transition: width 1s ease;
}
.money-footer {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-top: 4px;
}
.money-pct {
  font-size: 11px;
  color: var(--pink-dark);
  font-weight: 600;
}
.money-plans-count {
  font-size: 10px;
  color: var(--text-muted);
}

/* ===== 3. 各计划进度列表 ===== */
.plans-mini-list {
  margin: 0 12px 6px;
  background: var(--glass-bg);
  border: var(--glass-border);
  border-radius: var(--radius);
  padding: 12px 16px;
  box-shadow: var(--shadow-sm);
}
.pml-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 10px;
}
.pml-title { font-size: 13px; font-weight: 700; color: var(--text); }
.pml-more { font-size: 11px; color: var(--pink-dark); }
.pml-item { margin-bottom: 10px; }
.pml-item:last-child { margin-bottom: 0; }
.pml-top { display: flex; justify-content: space-between; font-size: 12px; margin-bottom: 4px; }
.pml-name { color: var(--text); font-weight: 500; }
.pml-amount { color: var(--pink-dark); font-weight: 600; }
.pml-bar {
  height: 5px;
  background: rgba(214, 92, 138, 0.12);
  border-radius: 3px;
  overflow: hidden;
}
.pml-bar-fill {
  height: 100%;
  background: linear-gradient(90deg, var(--pink), var(--pink-dark));
  border-radius: 3px;
  transition: width .8s ease;
}

/* ===== 4. 空状态 ===== */
.empty-state {
  margin: 0 12px 6px;
  background: var(--glass-bg);
  border: var(--glass-border);
  border-radius: var(--radius);
  padding: 24px 16px;
  text-align: center;
  box-shadow: var(--shadow-sm);
}
.empty-icon { font-size: 36px; display: block; }
.empty-text { font-size: 14px; color: var(--text-secondary); margin-top: 6px; display: block; }
.empty-sub { font-size: 12px; color: var(--pink-dark); margin-top: 4px; display: block; }

/* ===== 5. 纪念日 ===== */
.anni-row {
  margin: 0 12px 6px;
  background: var(--glass-bg);
  border: var(--glass-border);
  border-radius: var(--radius);
  padding: 12px 16px;
  display: flex;
  align-items: center;
  gap: 12px;
  box-shadow: var(--shadow-sm);
}
.anni-emoji { font-size: 20px; }
.anni-info { flex: 1; display: flex; justify-content: space-between; align-items: center; }
.anni-label { font-size: 12px; color: var(--text-secondary); }
.anni-days { font-size: 24px; font-weight: 800; color: var(--pink-dark); }
.anni-unit { font-size: 12px; font-weight: 400; color: var(--text-secondary); margin-left: 2px; }
.anni-arrow { font-size: 18px; color: var(--text-muted); }

/* ===== 6. 快捷入口 ===== */
.quick-row {
  display: flex;
  gap: 8px;
  padding: 0 12px;
  margin-bottom: 8px;
}
.qr-item {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 4px;
  background: #ffffff;
  border: 1px solid #ffd6e8;
  border-radius: var(--radius);
  padding: 12px 6px;
  box-shadow: var(--shadow-sm);
}
.qr-item:active { background: #fff5fb; }
.qr-emoji { font-size: 22px; }
.qr-label { font-size: 11px; color: var(--text-secondary); }

/* ===== 7. 底部数据 ===== */
.stats-bar {
  display: flex;
  align-items: center;
  background: var(--glass-bg);
  border: var(--glass-border);
  border-radius: var(--radius);
  margin: 0 12px 8px;
  padding: 10px 16px;
  gap: 0;
  box-shadow: var(--shadow-sm);
}
.sb-item { flex: 1; text-align: center; }
.sb-num { display: block; font-size: 14px; font-weight: 700; color: var(--pink-dark); }
.sb-label { display: block; font-size: 10px; color: var(--text-secondary); margin-top: 1px; }

/* ===== 8. 宠物展示区域 ===== */
.pet-area {
  margin: 0 12px 12px;
  background: linear-gradient(135deg, #fef9ff 0%, #fff0f8 100%);
  border: 1px solid #fad6e8;
  border-radius: var(--radius-lg);
  padding: 20px 16px 14px;
  text-align: center;
  position: relative;
  box-shadow: 0 4px 16px rgba(214, 92, 138, 0.08);
  overflow: hidden;
}
.pet-container {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 72px;
  height: 72px;
  border-radius: 50%;
  background: linear-gradient(135deg, #ffe4f0 0%, #ffd4e8 100%);
  box-shadow: 0 4px 12px rgba(214, 92, 138, 0.15);
  position: relative;
  z-index: 1;
}
.pet-emoji {
  font-size: 36px;
  display: block;
  line-height: 1;
  transition: transform 0.3s ease;
}
.pet-intimacy-text {
  display: block;
  font-size: 12px;
  font-weight: 600;
  color: var(--pink-dark);
  margin-top: 8px;
}
.pet-tip {
  display: block;
  font-size: 11px;
  color: var(--text-secondary);
  margin-top: 4px;
}
.pet-click-hint {
  position: absolute;
  right: 14px;
  top: 50%;
  transform: translateY(-50%);
  font-size: 11px;
  color: var(--text-muted);
  opacity: 0.6;
}

/* ===== 宠物动画 ===== */
/* 睡眠 - 轻微上下浮动 */
.pet-sleep .pet-emoji {
  animation: petBounce 3s ease-in-out infinite;
}
/* 慢走 - 左右慢速移动 */
.pet-walk-slow .pet-container {
  animation: petWalk 4s ease-in-out infinite;
}
/* 正常走 - 左右中速移动 */
.pet-walk .pet-container {
  animation: petWalk 2.5s ease-in-out infinite;
}
/* 兴奋 - 快速跳跃 */
.pet-excited .pet-container {
  animation: petJump 0.8s ease-in-out infinite;
}
/* 魔法 - 旋转+缩放 */
.pet-magic .pet-container {
  animation: petMagic 1.5s ease-in-out infinite;
}
.pet-magic .pet-emoji {
  animation: petGlow 1.5s ease-in-out infinite;
}

@keyframes petBounce {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-6px); }
}
@keyframes petWalk {
  0%, 100% { transform: translateX(-20px); }
  50% { transform: translateX(20px); }
}
@keyframes petJump {
  0%, 100% { transform: translateY(0) scale(1); }
  25% { transform: translateY(-12px) scale(1.08); }
  50% { transform: translateY(0) scale(1); }
  75% { transform: translateY(-6px) scale(1.04); }
}
@keyframes petMagic {
  0%, 100% { transform: translateY(0) rotate(0deg); }
  25% { transform: translateY(-8px) rotate(-8deg); }
  50% { transform: translateY(0) rotate(0deg); }
  75% { transform: translateY(-8px) rotate(8deg); }
}
@keyframes petGlow {
  0%, 100% { filter: brightness(1); transform: scale(1); }
  50% { filter: brightness(1.3); transform: scale(1.1); }
}

/* ===== 扭蛋机入口浮动按钮 ===== */
.gacha-btn {
  position: fixed;
  top: 12px;
  right: 12px;
  z-index: 999;
  width: 48px;
  height: 48px;
  border-radius: 50%;
  background: linear-gradient(135deg, #ff6b9d 0%, #c44d7a 100%);
  box-shadow: 0 4px 14px rgba(196, 77, 122, 0.35);
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
}
.gacha-btn:active {
  transform: scale(0.92);
  opacity: 0.85;
}
.gacha-icon {
  font-size: 22px;
  line-height: 1;
}
.gacha-badge {
  position: absolute;
  top: -4px;
  right: -4px;
  min-width: 18px;
  height: 18px;
  border-radius: 10px;
  background: #ff4757;
  color: #fff;
  font-size: 10px;
  font-weight: 700;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 0 4px;
  box-shadow: 0 2px 6px rgba(255, 71, 87, 0.4);
  line-height: 1;
}

/* ===== 宠物信息弹窗 ===== */
.modal-overlay {
  position: fixed;
  inset: 0;
  z-index: 1000;
  background: rgba(0, 0, 0, 0.45);
  display: flex;
  align-items: center;
  justify-content: center;
  animation: fadeIn 0.2s ease;
}
.pet-modal {
  background: #fff;
  border-radius: 20px;
  padding: 32px 24px 24px;
  width: 280px;
  text-align: center;
  position: relative;
  box-shadow: 0 20px 60px rgba(0, 0, 0, 0.15);
  animation: scaleIn 0.25s ease;
}
.pet-modal-emoji {
  font-size: 54px;
  display: block;
  margin-bottom: 6px;
}
.pet-modal-name {
  font-size: 20px;
  font-weight: 800;
  color: var(--text);
  display: block;
}
.pet-modal-level-row {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 6px;
  margin-top: 6px;
}
.pet-modal-level-label {
  font-size: 12px;
  color: var(--text-secondary);
}
.pet-modal-level-val {
  font-size: 14px;
  font-weight: 700;
  color: var(--pink-dark);
}
.pet-modal-bar-wrap {
  display: flex;
  align-items: center;
  gap: 8px;
  margin: 10px 0;
}
.pet-modal-bar {
  flex: 1;
  height: 6px;
  background: rgba(214, 92, 138, 0.15);
  border-radius: 3px;
  overflow: hidden;
}
.pet-modal-fill {
  height: 100%;
  background: linear-gradient(90deg, var(--pink), var(--pink-dark));
  border-radius: 3px;
  transition: width 0.6s ease;
}
.pet-modal-pct {
  font-size: 11px;
  font-weight: 600;
  color: var(--pink-dark);
  min-width: 36px;
  text-align: right;
}
.pet-modal-desc {
  display: block;
  font-size: 12px;
  color: var(--text-secondary);
  line-height: 1.6;
  margin-top: 6px;
}
.pet-modal-btn {
  margin-top: 16px;
  width: 100%;
  height: 42px;
  line-height: 42px;
  background: linear-gradient(135deg, var(--pink), var(--pink-dark));
  color: #fff;
  font-size: 14px;
  font-weight: 600;
  border: none;
  border-radius: 21px;
  box-shadow: 0 4px 12px rgba(214, 92, 138, 0.3);
}
.pet-modal-btn:active {
  opacity: 0.85;
  transform: scale(0.97);
}
.pet-modal-close {
  position: absolute;
  top: 12px;
  right: 16px;
  font-size: 18px;
  color: var(--text-muted);
  padding: 4px 8px;
  cursor: pointer;
}

@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}
@keyframes scaleIn {
  from { opacity: 0; transform: scale(0.85); }
  to { opacity: 1; transform: scale(1); }
}
